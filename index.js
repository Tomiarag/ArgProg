

function onClickMenu(menuItem) {

    // buscar todos los elementos que tengan la clase  "bokita"
    // iterar x los elementos de la lista
    // eliminar clase bokita del os elementos que la tengan asignada


    var element = document.getElementById(menuItem)
    element.classList.add("bokita")
}